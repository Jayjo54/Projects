** start of script.js **

function confirmEnding (stringChecker, stringEnd) {
  let strEnding = stringChecker.lastIndexOf(stringEnd); 
  let strCheck2 = stringChecker.slice(strEnding, stringChecker.length);
  if(stringEnd === strCheck2) {
    console.log(strCheck2);
    return true;
  } else {
    console.log(strCheck2);
    return false;
  }
}

console.log(confirmEnding("Abstraction", "action"));
console.log(confirmEnding("Connor", "n"))
console.log(confirmEnding("Congratulation","on"));

** end of script.js **

